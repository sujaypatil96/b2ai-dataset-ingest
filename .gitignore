# All raw inputs, real and synthetic. data/real/ is licensed source data under a DUA;
# data/synthetic/ is generator checkouts and public stand-ins. Neither is committed.
/data/*
!/data/.gitkeep

# Real Bridge2AI-Voice PhysioNet download — credentialed access, HIPAA/PII-sensitive.
# Must never be committed or pushed.
/physionet.org/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Output artifacts
/out/

# Browser-automation scratch (Playwright MCP page/console dumps)
.playwright-mcp/

# Stratiphy's downloaded HPO build (~22 MB). Deliberately NOT under data/, whose
# default it would otherwise take: `stratiphy setup download -d` defaults to ./data,
# which here is the protected input tree.
/.stratiphy/

# OS / editor
.DS_Store
.idea/
.vscode/
